/*'use client';
import React from "react";
import { v1 as uuid } from "uuid";

const CreateRoom = () => {

    const createRoom = () => {
        const id = uuid();
        window.location.href = `/room/${id}`;
    }

    return (
        <div className="flex justify-center items-center h-screen">
            <button onClick={createRoom} className="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded">
                Create Room
            </button>
        </div>
    );
};

export default CreateRoom;*/
